</br>
  <div class="container">
    <h1>New game</h1>
      <div class="container">
        <div class="row">
          <% @letters.each do |letter| %>
          <div class="col-1 card"><%= letter %></div>
          <% end %>
        </div>
      </div>
      <div class="container">
        <form action="score">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <p>What's the longest work you can find?</p>
          <input type="text" name="answer">
          <input type="submit" value="Play">
        </form>
      </div>
    </div>
